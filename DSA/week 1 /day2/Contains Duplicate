class Solution {
public:
    bool containsDuplicate(vector<int>& arr) {
        unordered_set<int> seen;
        for(int x:arr){
            if(seen.find(x)!=seen.end()){
                return true;
            }
            seen.insert(x);
        }
        return false;
        
    }
};
